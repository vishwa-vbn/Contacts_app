<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <!-- Date and Time TextView -->
    <TextView
        android:id="@+id/date_timeTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sep 01, 14:39"
        android:textSize="16sp"
        android:textColor="#000000"
        android:layout_alignParentStart="true" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/date_timeTextView"
        android:layout_marginTop="8dp"
        >
    <!-- Phone Number TextView -->
    <TextView
        android:id="@+id/phoneNumberTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        android:text="Phone: +1234567890"
        android:textSize="14sp"
        android:textColor="#000000" />

    <!-- Incoming/Outgoing Call Icon -->
    <ImageView
        android:id="@+id/callIconImageView"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_toRightOf="@id/phoneNumberTextView"
        android:layout_centerVertical="true"
        android:layout_marginStart="5dp"
        android:src="@drawable/baseline_arrow_inward_24"
        />
    </LinearLayout>

</RelativeLayout>
